debug:
  enabled: true

dataset:
  name: fakedata
  data_dir: data
  download: false
  fake_data_on_missing: true
  num_workers: 0
  batch_size: 32
  test_batch_size: 128
  max_train_samples: 64
  max_test_samples: 64

federated:
  num_clients: 5
  clients_per_round: 3

training:
  rounds: 1
  local_epochs: 1
  lr: 0.01
  seed: 123

attack:
  name: none

defense:
  name: fedavg
